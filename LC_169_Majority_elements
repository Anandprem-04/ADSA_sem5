APPROACH : Initiate a for loop and if we find the same element count++ else negate it , and if count goes 0 then replace the element with that element having 0 frequency

class Solution {
    public int majorityElement(int[] nums) {
        int e = nums[0];
        int count =0;
        for(int c : nums){
            if (count == 0) {
                e = c;
                count = 1;
            } else if (c == e) {
                count++;
            } else {
                count--;
            }
        }
        return e;
    }
}

Elements in the array can be different not only 2 but one of the elements have a frequency of N/2 , there we have to count for frequency of the element 
Example 1:
Input: nums = [2,2,1,1,1,2,2]
Output: 2
